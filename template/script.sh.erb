#!/usr/bin/env bash

# Benchmark info
echo "TIMING - Starting main script at: $(date)"

# Set working directory to home directory
cd "${HOME}"

source <%= context.version %>
#<= context.jupyterlab_switch == "1" ? "lab" : "notebook" >
