<%-
  base_slurm_args = []
  slurm_args = base_slurm_args + resource.split
-%>
---
batch_connect:
  template: "basic"
  conn_params:
    - jupyter_api
    - job_stats_link
script:
  native:
    <%- slurm_args.each do |arg| %>
    - "<%= arg %>"
    <%- end %>
