<%-
  base_slurm_args = []
  slurm_args = base_slurm_args + ["--gpus-per-node", node_type]
-%>
---
batch_connect:
  template: "basic"
  conn_params:
    - jupyter_api
script:
  native:
    <%- slurm_args.each do |arg| %>
    - "<%= arg %>"
    <%- end %>
